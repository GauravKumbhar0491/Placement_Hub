document.addEventListener("DOMContentLoaded", function () {
    // Trigger the fade-in animation for the company list
    var companyList = document.getElementById("companyList");
    companyList.style.opacity = 1;
});
